@model IEnumerable<MVCPartialViewDemo.Models.tblEmployee2>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model)
{
    @Html.Partial("_Employee", item)
    { Html.RenderPartial("_Employee", item); }
}